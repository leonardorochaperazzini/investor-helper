# Database connection (used by data-migration/knexfile.ts)
DB_HOST=localhost
DB_PORT=5432
DB_USER=invest
DB_PASSWORD=invest
DB_NAME=invest

# Alternatively, you can use a single connection string.
# If set, DATABASE_URL overrides the individual DB_* vars above.
# DATABASE_URL=postgres://invest:invest@localhost:5432/invest
